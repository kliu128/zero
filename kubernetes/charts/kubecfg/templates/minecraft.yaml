apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: minecraft
spec:
  selector:
    matchLabels:
      app: minecraft
  template:
    metadata:
      labels:
        app: minecraft
    spec:
      containers:
        - name: minecraft
          image: itzg/minecraft-server:latest
          env:
            # Accept Mojang EULA (required)
            - name: EULA
              value: "TRUE"
            # Set Minecraft version and opts
            - name: VERSION
              value: "1.13.2"
            - name: TYPE
              value: VANILLA
            # - name: FORGEVERSION
            #   value: 12.18.3.2477
            - name: MEMORY
              value: 1G
            # Minecraft config
            - name: DIFFICULTY
              value: hard
            - name: JVM_XX_OPTS
              value: -XX:+UseG1GC -XX:+UnlockExperimentalVMOptions -XX:MaxGCPauseMillis=100 -XX:+DisableExplicitGC -XX:TargetSurvivorRatio=90 -XX:G1NewSizePercent=50 -XX:G1MaxNewSizePercent=80 -XX:G1MixedGCLiveThresholdPercent=35 -XX:+AlwaysPreTouch -XX:+ParallelRefProcEnabled -Dusing.aikars.flags=mcflags.emc.gs
            - name: OPS
              value: nitroignika,WildTaegr
            - name: ENABLE_QUERY
              value: "TRUE"
            - name: ANNOUNCE_PLAYER_ACHIEVEMENTS
              value: "TRUE"
            - name: MOTD
              value: "Kevloo Hosting"
          ports:
            - containerPort: 25565
              name: minecraft
          volumeMounts:
            - name: data
              mountPath: /data
          # Allow for attaching
          # tty: true
          # stdin: true
      nodeSelector:
        kubernetes.io/hostname: rem
      hostNetwork: true
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: minecraft-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    app: minecraft
  name: minecraft-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
status: {}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: minecraft
  name: minecraft
spec:
  ports:
  - name: mc
    port: 25565
    targetPort: 25565
  selector:
    app: minecraft
---