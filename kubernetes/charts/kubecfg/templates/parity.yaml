apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: parity-kovan
  name: parity-kovan
spec:
  selector:
    matchLabels:
      app: parity-kovan
  template:
    metadata:
      labels:
        app: parity-kovan
    spec:
      nodeSelector:
        kubernetes.io/hostname: rem
      hostNetwork: true
      containers:
      - image: parity/parity:beta
        imagePullPolicy: Always
        args: [-d, /data, --no-ancient-blocks,--chain=kovan]
        name: parity
        volumeMounts:
        - mountPath: /data
          name: data
      restartPolicy: Always
      volumes:
      - name: data
        hostPath:
          type: Directory
          path: /mnt/ssd/parity-kovan